<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:tag="rootview"
    tools:context="com.sampleapp.HomeActivity$SetTwo" >

    <EditText
        android:id="@+id/city_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/city_hint"
        android:maxLines="1"
        android:inputType="textCapWords"
        android:tag="cityTextfield" />

    <Button
        android:id="@+id/add_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/city_text"
        android:layout_below="@+id/city_text"
        android:tag="addButton"
        android:text="@string/add_button" />

    <ExpandableListView
        android:id="@+id/cityListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/city_text"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/add_button"
        android:layout_below="@+id/add_button"
        android:layout_marginTop="33dp"
        android:tag="cityListView" />

</RelativeLayout>